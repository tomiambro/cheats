% tests

$ compose_file: docker-compose.yml


# Run tests with pytest inside the container
docker-compose run -f <compose_file> pytest

# Run tests with coverage
docker-compose run -f <compose_file> coverage run --source=app -m pytest -v
docker-compose run -f <compose_file> coverage report -m
